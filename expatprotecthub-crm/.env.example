# Copy to .env and fill in from your Supabase project (Settings -> API).
# The anon key is public by design; access control is enforced by RLS in the database.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
